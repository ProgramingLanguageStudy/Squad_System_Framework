# 📅 개발일지

---

## 2026-03-13 (금요일)

### 오늘 한 일

- **Intro: Game Start 전환 연출 추가 (edbddc9)**  
  IntroSceneView에 `PlayTransitionToPlayScene` 추가. 패널 SetActive(false) → 배경 스케일 1→1.2 애니메이션 (ease-out) → 연출 완료 후 LoadPlayScene 호출.

- **Boot 씬 추가 (841567e)**  
  GameManager 준비 단계용 Boot 씬. 앱 실행 시 1회 사용.

- **Scene: SceneLoadManager, SceneTransitionLoadingView (4f60fc4)**  
  씬 전환 로딩 UI. DataManager/ResourceManager 초기화, Play 씬 로드. OnSceneReady 후 로딩 숨김.

- **Intro: View 분리 및 Auth 서비스 (005648c)**  
  TitleView, LoginView, MainView, ErrorView, LoadingView 분리. IntroAuthService 추가. GameManager SceneLoadManager, PlayScene OnSceneReady 연동.

- **UI: Tween Preset 구조 (630d56b)**  
  IUITweenBase 계열 삭제, UIPresetData·PanelPreset·TitlePreset·ToastPreset·UIRole 추가. IBlocksInput, IRespondsToEsc. UITweenFacadeEditor.

- **씬: Intro·Play 업데이트 (00e5603)**  
  배경 참조, Boot 연동 등 씬 설정 반영.

### 기술 메모

- **Boot 구조**: 앱 실행 시 1회, GameManager 생성·DontDestroyOnLoad. DM/RM preload 시작만 하고 완료는 콜백. 바로 Logo/Intro 로드.
- **Intro 전환 연출**: 배경만 남기고 Scale Up(몰입감). pivot (0.5, 0.5) 기준 확대.
- **씬 전환 UI**: GameManager 하위 Canvas(Screen Space - Overlay)로 DontDestroy. 씬 전환 후에도 유지.

### 배운 점

- Boot와 Logo/Intro 분리 시, "타이틀로 돌아가기"는 Logo만 로드. Boot는 재진입 안 함.
- Boot는 Awake/Start만 처리, 비동기는 시작만 하고 완료는 콜백. 거의 1~2프레임 수준.
- Boot 씬에 카메라 없으면 "No cameras rendering" → 빌드에서도 보일 수 있으니 Main Camera 추가 권장.

### 내일(집에서) 할 일

- [ ] Boot 씬에서 GameManager 생성 후 Logo/Intro 씬 로드하도록 구현
- [ ] Boot에서 DataManager, ResourceManager preload 시작 (완료 대기 X)
- [ ] Build Settings에 Boot 씬을 0번(첫 씬)으로 등록
- [ ] 씬 전환 UI: GameManager 하위 Canvas 구성 (DontDestroyOnLoad)
- [ ] (선택) 씬 전환 UI 프리팹화 → ResourceManager 캐싱 → SceneLoadManager 참조
- [ ] IntroSceneView 배경 이미지 인스펙터 연결 확인 및 테스트

### 기타

- Logo 씬은 추후 추가 예정 (Boot → Logo → Intro 구조).

---

*Git 로그 기준 정리 (2026-03-13).*

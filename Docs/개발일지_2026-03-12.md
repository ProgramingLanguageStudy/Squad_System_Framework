# 📅 개발일지

---

## 2026-03-12 (목요일)

### 오늘 한 일

- **UI Tween 시스템 추가 (fa88f7d)**  
  IUITweenBase, UITweenBase, UITweenFacade 추가. ScaleTween, AlphaTween, PositionTween 구현. UIAnimator 제거, Facade 기반 구조로 전환.

- **View들 UITweenFacade 연동 (e7f3a74)**  
  SettingsView, MapView, InventoryView, IntroSceneView, InteractionUI, InventoryTooltipView, QuestView에 UITweenFacade 적용. 등장/퇴장 연출, playEnterOnStart·controlActive 옵션 사용.

- **스크립트 폴더 구조 정리 (c4897d0)**  
  PortalSlot: UI/ → Map/. ItemPickupLog, ItemPickupSlot: UI/ → Reward/.

- **Docs: UI 구조·연출 가이드, 세이브 설계 추가 (4dc5f86)**  
  UI구조_가이드(UI 종류, 입력 정책, IBlocksInput/IRespondsToEsc, UIRegistry). UI연출_적용_가이드(UITweenFacade 예시). 세이브_서버연동_설계(Firestore 연동).

- **Save: Firestore 백엔드·서버 연동 (11f6225)**  
  FirestoreSaveBackend 추가. SaveSystem 제거, SaveManager·SquadSaveContributor 수정. PlaySaveCoordinator, PlayScene, GameManager 연동.

- **기타: FlagDebugger, SquadController 수정 (956f254)**  
  플래그 디버거 및 분대 컨트롤러 업데이트.

### 기술 메모

- **UITweenFacade**: IUITweenBase 구현체(Scale, Alpha, Position)를 묶어 등장/퇴장 제어. `playEnterOnStart`, `controlActive`로 초기 상태·SetActive 자동 제어.

- **폴더 구조**: 포탈·맵 관련은 Map/, 획득 피드백 UI는 Reward/로 분리해 관심사별 배치.

- **IBlocksInput / IRespondsToEsc**: UI구조_가이드에 입력 정책, UIRegistry 패턴 정리.

- **FirestoreSaveBackend**: SaveSystem 제거 후 SaveManager가 백엔드 교체 가능 구조. PlaySaveCoordinator가 PlayScene·세이브 흐름 조율.

### 배운 점

- UITween 기반으로 등장/퇴장 일관 적용 시, Facade 패턴으로 여러 Tween을 한 번에 제어하면 View 코드가 단순해짐.

### 내일 할 일

- Firestore 실연동 테스트 (회원가입·로그인 후 세이브/로드)
- UI 연출 추가 적용·튜닝 (필요 시)

### 기타

- 

---

*Git 로그 기준 정리 (2026-03-12).*

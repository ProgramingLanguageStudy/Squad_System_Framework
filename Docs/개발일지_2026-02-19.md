# 📅 개발일지

---

## 2026-02-19 (목요일)

### 오늘 한 일

- **폴더명 정리 (b0139fc)**  
  Enemies→Enemy, Items→Item, Portals→Portal로 폴더명 변경.

- **Character 시스템 추가 (83b3ffe)**  
  Character 통합: Model, Mover, FollowMover, Animator, Interactor, Attacker, StateMachine. CharacterInteractor에 RequireComponent 적용, SetAsPlayer/SetAsCompanion 시 enabled 토글. CharacterFollowMover로 동료용 따라가기 이동.

- **Player → PlayerController 병합 (cbb44df)**  
  Player 클래스 삭제, 로직 PlayerController로 통합. PlayerSaveHandler, PlayerDebugger, InventoryPresenter 참조 변경. Portal, PortalController, RespawnController 정리.

- **Q키 SquadSwap 및 SquadController (56890a2)**  
  InputHandler: Quest 입력 → SquadSwap 액션. SquadController: 분대 구성, 현재 조종 캐릭터 교체. PlayScene: SquadController 통합, OnCurrentControlledChanged 이벤트.

- **PlaySceneView 추가, PlayerHealthBarView 제거 (2323be0)**  
  PlaySceneView로 현재 조종 캐릭터 체력바 표시, Model.OnHpChanged 구독. PlayerHealthBarView 삭제.

- **EnemySpawner SetChaseTarget (f21afc4)**  
  현재 조종 캐릭터를 추적 타겟으로 설정. GameEvents 수정.

- **Items Character 기반 수정 (fb95784)**  
  IItemUser, BuffItemData, EquipmentItemData, ItemObject — Player → Character 참조로 변경.

- **Prefabs, Play 씬, Animators 정리 (d773f38)**  
  Player 프리팹 제거, Character 프리팹 반영. Play.unity 씬 업데이트. PicoChan, SapphiChan Animator override controller 추가.

- **Npc, PortalMenuView, InputSystem 수정 (7a141c8)**  
  Character/PlayerController 참조 정리. InputSystem SquadSwap 액션 바인딩.

- **git-commit-by-parts 규칙 추가 (c4f1c79)**  
  논리적 단위로 파트별 커밋 안내 규칙 추가.

- **use-system-date 규칙 추가**  
  날짜 관련 요청 시 대화 컨텍스트 대신 시스템 날짜(Get-Date) 사용 규칙 추가.

- **세이브데이터·에셋·AI 프롬프트 (f716629, 1fb6d7d, cc9c675)**  
  SaveData 갱신, 다운받은 에셋 수정, AI용 프롬프트 추가. (직접 처리)

### 기술 메모 / 배운 점

- **파트별 커밋**: 한 번에 많은 변경을 한 커밋에 넣지 않고, Character 통합 / Player 병합 / SquadSwap / PlaySceneView처럼 논리적 단위로 나눠 커밋하면 히스토리 추적·롤백이 쉬움. `git-commit-by-parts` 규칙 참고.

- **한글 커밋 메시지**: PowerShell `git commit -m "한글"`은 인코딩 때문에 깨질 수 있음. UTF-8 `msg.txt`를 만들고 `git commit -F msg.txt`로 커밋하면 안전함. (`git-commit-korean-encoding` 규칙)

- **Cursor의 "오늘" 날짜**: Cursor가 전달하는 "Today's date"는 대화 시작 시점에 고정됨. 며칠 지난 대화에서는 실제 오늘과 다를 수 있음. 날짜 관련 요청 시 `Get-Date`로 시스템 날짜를 먼저 조회하는 `use-system-date` 규칙을 두면 해결됨.

- **AI는 날짜를 스스로 업데이트 못함**: AI가 저장하는 값이나 컨텍스트를 직접 바꿀 수 없음. 대신 터미널로 `Get-Date` 실행해 현재 날짜를 조회하는 방식 사용.

- **개발일지 기술 메모**: "기술 메모 / 배운 점"은 AI가 아니라 **내가** 작업하면서 배운 걸 적는 섹션. AI가 개발일지를 작성할 때 이 부분은 비워두거나 내가 채우는 게 맞음.

- **개발일지 형식**: `Docs/개발일지_템플릿.md`와 기존 일지(예: 2026-02-12, 2026-02-13) 형식에 맞춰야 함. 날짜(YYYY-MM-DD), 요일, 각 항목별 커밋 해시 포함.

### 내일 할 일 (선택)

-

### 기타

-

---

*Git 로그 기준 정리 (2026-02-19 커밋).*

public enum QuestType { Gather, Kill, Visit } // ����, óġ, �湮
public enum QuestState { CanStart, InProgress, CanComplete, Completed }

public enum DialogueType
{
    Quest,
    Common,
    Affection,
}